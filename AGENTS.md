# 项目约束

## 沟通语言

- 会话默认使用中文。
- 用户明确要求其他语言时，再切换到对应语言。

## 文档语言

- 新增和修改的项目文档默认使用中文。
- 代码标识、命令、文件路径、协议字段和外部标准名称保持原文。
- 如果必须引用英文来源，应使用中文解释其结论。

## 终端输出

- 终端命令输出默认限制在 15 行以内。
- 预计会产生长输出的命令，必须在命令本身限制输出，例如 PowerShell 使用 `Select-Object -First 15`。
- 需要查看完整输出时，优先改用更精确的查询条件；只有用户明确要求完整输出时才展示超过 15 行的内容。

## 当前产品约束

- 项目第一阶段保持纯静态 HTML 页面。
- 不引入 npm、Playwright、后端服务或构建流程，除非用户后续明确调整。
- 浏览器验证由用户手动完成。
- 战术内容应逐步迁移为标准结构化数据，优先支持 IndexedDB 本地内容库和 JSON 导入注册。
